"use strict";
function showInfo(user) {
    console.log('User info', user.id, user.name, user.lastName);
}
function showThatInfo(user) {
    console.log('User info', user.id, user.name, user.lastName);
}
let uwu;
